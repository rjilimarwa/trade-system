<form action="{{ path('recherche') }}" method="post" class="navbar-form navbar-left">
    {{ form_widget(form.recherche) }}
    <button type="submit" class="btn btn-default">Chercher !</button>
    {{ form_widget(form) }}
</form>